import * as stylex from "@stylexjs/stylex";

export const clientBackground = "rgb(151, 92, 179)";

export const clientStyles = stylex.create({
  control: { backgroundColor: clientBackground, color: "white", padding: 12 },
});
